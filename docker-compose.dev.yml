version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fastapi_dev
    env_file:
      - env/dev/.env
    environment:
      APP_ENV: dev
      KAGGLE_CONFIG_DIR: /root/.kaggle
      DATA_DIR: /app/data
      REDIS_HOST: redis
    volumes:
      - ./.kaggle:/root/.kaggle:ro
      - ./data:/app/data
      - ./app:/app/app
      - ./run_seed.py:/app/run_seed.py
      - ./env:/app/env:ro
    ports:
      - "80:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      redis:
        condition: service_healthy
    restart: always

  # Redis
  redis:
    image: redis:7
    container_name: redis_dev
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 30
    restart: always

volumes:
  redis_data:
